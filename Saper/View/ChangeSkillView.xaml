<Window x:Class="Saper.View.ChangeSkillView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Saper.View"
        mc:Ignorable="d"
        Title="ChangeSkillView" Height="450" Width="800">
    <Grid>
        <ListBox ItemsSource="{Binding SkillsList}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding SkillImage}" Height="100" Width="100" Stretch="Fill"/>
                        <Label Content="{Binding SkillName}" />
                        <Label Content="{Binding Description}" />
                        <Button Content="Select" 
                                Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                CommandParameter="{Binding SkillName}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
