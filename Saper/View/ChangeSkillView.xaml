<Window x:Class="Saper.View.ChangeSkillView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Saper.View"
        mc:Ignorable="d"
        Title="ChangeSkillView" Height="450" Width="800">
    <Grid>
        <ListBox ItemsSource="{Binding SkillsList}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="{Binding SkillImage}" Height="100" Width="100" Stretch="Fill" />
                        <Label Grid.Column="1" Content="{Binding SkillName}" Style="{StaticResource MainLabelStyle}"/>
                        <Label Grid.Column="2" Content="{Binding Description}" Style="{StaticResource MainLabelStyle}"/> <!-- TODO - fix resources -->
                        <Button Grid.Column="3" Content="Select"
                                Style="{StaticResource MainButtonStyle}"
                                Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                CommandParameter="{Binding SkillName}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
